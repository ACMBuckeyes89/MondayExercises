package Exercises;

import java.awt.*;
import java.awt.geom.*;
import javax.swing.*;

public class Challenge {

    public static void main(String[] args) {
        int a = 42;
        int b = 43;

        //Using the max method from Math
        System.out.println(Math.max(a, b));

        //Using addExact to return the sum of arguments
        System.out.println(Math.addExact(a,b));

        //Using multiplyExact to return the product of arguments
        System.out.println(Math.multiplyExact(a,b));
    }
}









































